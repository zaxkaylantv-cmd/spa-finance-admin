
# Local runtime data (never commit)
backend/data/

# Local WIP / recovery artefacts (never commit)
README.codex-overwrite-*.md
_wip-before-readme-restore-*.patch

# Secrets
.env

# Local WIP patches
_wip-*.patch

# Local env files (never commit)
backend/.env
frontend/.env
*.env.backup-*
